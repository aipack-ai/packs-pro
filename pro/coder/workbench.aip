# Before All

```lua
return aip.flow.before_all_response({
	inputs = {},
	before_all = {
		success = false,
		error_msg = "The `pro@coder/workbench` agent is no longer a supported sub-agent implementation path. Configure root `workbench:` in the coder prompt instead."
	}
})

```

# After All

```lua
return before_all
```
